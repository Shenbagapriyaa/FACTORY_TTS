<div class="panel form-panel"><form method="POST" action="{{ $action }}">@csrf @if($method!=='POST') @method($method) @endif<div class="form-grid"><div class="field"><label>Group Code *</label><input name="group_code" value="{{ old('group_code',$fabricGroup?->group_code) }}" required></div><div class="field"><label>Group Name *</label><input name="group_name" value="{{ old('group_name',$fabricGroup?->group_name) }}" required></div><div class="field full-field"><label>Description</label><textarea name="description">{{ old('description',$fabricGroup?->description) }}</textarea></div><div class="field"><label>Status *</label><select name="status"><option @selected(old('status',$fabricGroup?->status??'Active')==='Active')>Active</option><option @selected(old('status',$fabricGroup?->status)==='Inactive')>Inactive</option></select></div></div><div class="selection"><div class="selection-head"><h3>Select Fabrics *</h3><span>Choose one or more</span></div><div class="check-grid">@foreach($fabrics as $fabric)<label class="check"><input type="checkbox" name="fabric_ids[]" value="{{ $fabric->id }}" @checked(in_array($fabric->id, old('fabric_ids',$selectedFabricIds)))><span><strong>{{ $fabric->fabric_code }}</strong> — {{ $fabric->fabric_name }}<small>{{ $fabric->fabric_type }} · {{ $fabric->gsm ?? '-' }} GSM</small></span></label>@endforeach</div></div><div class="form-actions"><a class="btn" href="{{ route('fabric-groups.index') }}">Cancel</a><button class="btn primary">Save Group</button></div></form></div>
