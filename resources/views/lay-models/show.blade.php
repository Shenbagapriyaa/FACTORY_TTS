@extends('layouts.app') @section('page-title','Lay Model Details') @section('page-subtitle',$layModel->lay_model_code.' · '.$layModel->lay_model_name) @section('content')<div class="panel"><div class="detail-grid"><div><span>Code</span><strong>{{ $layModel->lay_model_code }}</strong></div><div><span>Name</span><strong>{{ $layModel->lay_model_name }}</strong></div><div><span>Fabric Group</span><strong>{{ $layModel->fabricGroup->group_code }} — {{ $layModel->fabricGroup->group_name }}</strong></div><div><span>Fabric</span><strong>{{ $layModel->fabric->fabric_code }} — {{ $layModel->fabric->fabric_name }}</strong></div><div><span>Lay Length</span><strong>{{ $layModel->lay_length }}</strong></div><div><span>Lay Width</span><strong>{{ $layModel->lay_width }}</strong></div><div><span>Number of Plies</span><strong>{{ $layModel->number_of_plies }}</strong></div><div><span>Garment Size</span><strong>{{ $layModel->garment_size ?: '-' }}</strong></div><div><span>Marker Length</span><strong>{{ $layModel->marker_length ?? '-' }}</strong></div><div><span>Marker Width</span><strong>{{ $layModel->marker_width ?? '-' }}</strong></div><div><span>Status</span><strong><span class="status {{ strtolower($layModel->status) }}">{{ $layModel->status }}</span></strong></div></div><div class="description"><span>Description</span><p>{{ $layModel->description ?: 'No description.' }}</p></div><div class="form-actions"><a class="btn" href="{{ route('lay-models.index') }}">Back</a><a class="btn primary" href="{{ route('lay-models.edit',$layModel) }}">Edit</a></div></div>@endsection
